#include <stdio.h>
#include <stdlib.h>

int main(){
	int type_num;
	scanf("type num of childs :");
	
	/*for(int i = 0 ; i < type_num ; i++){
		pid[i] = fork();
		printf("[child %d] : Started! pid = %d, sleep=%d", i, getpid(), i+1);
		sleep(i+1);
	}
	*/
}
